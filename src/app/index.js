

export const application = (blockchainService) = async () => {

    try {
        const blockchain = this.blockchainService.createBlockchain()
    } catch (error) {
        console.error(error);
    }
}